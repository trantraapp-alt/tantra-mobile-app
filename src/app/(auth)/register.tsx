// Route binding for the registration screen.
import { RegisterScreen } from '@/features/auth';

// Renders the registration screen at /(auth)/register.
export default function RegisterRoute() {
  return <RegisterScreen />;
}
