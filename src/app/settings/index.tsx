// Route binding for the settings screen.
import { ComingSoonScreen } from '@/components/shared';

// Renders the settings screen at /settings.
export default function SettingsRoute() {
  return (
    <ComingSoonScreen
      title="Settings"
      description="Account, address and preference settings will appear here."
    />
  );
}
