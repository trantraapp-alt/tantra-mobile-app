// Route binding for the wishlist tab.
import { WishlistScreen } from '@/features/wishlist';

// Renders the wishlist screen at /(tabs)/wishlist.
export default function WishlistRoute() {
  return <WishlistScreen />;
}
