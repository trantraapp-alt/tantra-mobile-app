// Route binding for the seller's My Listings screen.
import { MyListingsScreen } from '@/features/listings';

// Renders the My Listings screen at /listings.
export default function MyListingsRoute() {
  return <MyListingsScreen />;
}
