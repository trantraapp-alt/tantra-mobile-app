// Route binding for a module's category screen (opened from the Sell sheet).
import { SellCategoriesScreen } from '@/features/sell';

// Renders the sell categories screen at /sell/[moduleId].
export default function SellCategoriesRoute() {
  return <SellCategoriesScreen />;
}
