// Route binding for the login screen.
import { LoginScreen } from '@/features/auth';

// Renders the login screen at /(auth)/login.
export default function LoginRoute() {
  return <LoginScreen />;
}
