// Route binding for a seller's public storefront at /seller/[userId].
import { SellerProfileScreen } from '@/features/marketplace';

// Renders the seller profile screen.
export default function SellerRoute() {
  return <SellerProfileScreen />;
}
