// Route binding for the profile tab.
import { ProfileScreen } from '@/features/profile';

// Renders the profile screen at /(tabs)/profile.
export default function ProfileRoute() {
  return <ProfileScreen />;
}
