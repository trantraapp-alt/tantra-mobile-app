// Route binding for the deal-group listings screen at /deals/[groupKey].
import { DealListingsScreen } from '@/features/deals';

// Renders the deal listings screen.
export default function DealListingsRoute() {
  return <DealListingsScreen />;
}
