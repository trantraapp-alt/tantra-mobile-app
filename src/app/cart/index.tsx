// Route binding for the cart screen (reachable from the home cart button).
import { CartScreen } from '@/features/cart';

// Renders the cart screen at /cart.
export default function CartRoute() {
  return <CartScreen />;
}
