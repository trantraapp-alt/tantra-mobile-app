// Route binding for the buyer listing detail at /listing/[id].
import { MarketplaceListingDetailScreen } from '@/features/marketplace';

// Renders the buyer listing-detail screen (with contact reveal).
export default function ListingDetailRoute() {
  return <MarketplaceListingDetailScreen />;
}
