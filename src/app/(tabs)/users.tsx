// Route binding for the admin-only Users tab.
import { UsersScreen } from '@/features/admin-users';

// Renders the users screen at /(tabs)/users.
export default function UsersRoute() {
  return <UsersScreen />;
}
