# Public runtime configuration consumed via Expo Constants / process.env.
# Copy to `.env` and adjust per environment.
EXPO_PUBLIC_API_BASE_URL=https://automaker-passion-unworried.ngrok-free.dev/api/v1
EXPO_PUBLIC_API_TIMEOUT_MS=20000
EXPO_PUBLIC_ENVIRONMENT=development
