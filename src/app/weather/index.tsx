// Route binding for the weather + farming advisory screen at /weather.
import { WeatherScreen } from '@/features/home';

// Renders the weather screen.
export default function WeatherRoute() {
  return <WeatherScreen />;
}
